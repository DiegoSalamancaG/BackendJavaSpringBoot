# Nombre de la aplicación
spring.application.name=backend-springboot

# Usar el puerto 8081 para evitar conflictos con el 8080
server.port=8081

# Configuración de la base de datos
spring.datasource.url=jdbc:postgresql://localhost:5432/e-commerce
spring.datasource.username=postgres
spring.datasource.password=Asdf1474

# Configuración de JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Desactivar la configuración automática de seguridad
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# Configuración del JWT
jwt.secret=unaClaveMuySegura12345678901234567890
# 2 horas en milisegundos
jwt.expiration=7200000
